# Callbacks Testing

Use `done` callback to signal completion. `test('', done => { ... done(); })`. Error if `done` not called.

> 📘 Next: [Promises & resolves/rejects](02-promises-and-resolves-rejects.md)
